const nome = 'DeadMaul';

if (nome === 'Guilherme' || nome === 'Gui' || nome === 'DeadMaul'){
    console.log(`Acesso permitido. Bem-vindo ${nome}`);
} else{
    console.log(`Acesso negado. seu nome é ${nome}`)
}